#include <stdio.h>

int main(){
    short number = 23651; // shorty is a data type of Size: Typically 2 bytes (16 bits)
    printf("%hd", number);  // "%ld" is a format specifier for long data type 

    return 0;
}

// Output : 23651

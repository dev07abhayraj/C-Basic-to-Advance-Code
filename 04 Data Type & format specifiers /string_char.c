#include <stdio.h>

int main(){
    char name[20] = "Abhayraj";  // char is a data type and [20] this represents the array of length 20 means 20 char can be ˀˀstore in it 
    printf("%s", name); // "%s" is a format specifier used to print string or array of char 
    return 0;
}

// Output : Abhayraj
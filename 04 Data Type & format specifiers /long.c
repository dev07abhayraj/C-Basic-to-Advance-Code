#include <stdio.h>

int main(){
    long mobile_number = 6263495051; // long is a data type which is used to get more big values of 10-25 digits that int can't handle
    printf("%ld", mobile_number);  // "%ld" is a format specifier for long data type 
    return 0;
}

// Output : 6263495051
